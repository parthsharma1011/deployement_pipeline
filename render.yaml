services:
  - type: web
    name: linkedin-generator
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    port: 10000
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
